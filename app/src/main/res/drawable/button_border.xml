<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Màu nền của button -->
    <corners android:radius="8dp" /> <!-- Bo góc cho button -->
    <stroke
        android:width="2dp"
        android:color="@color/black" />
</shape>
